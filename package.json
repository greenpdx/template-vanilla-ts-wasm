{
  "name": "vite-typescript-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "wasm-pack build wasm -d ../pkg && tsc && vite build",
    "preview": "vite preview",
    "wasm": "wasm-pack build wasm -d ../pkg"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4",
    "vite-plugin-top-level-await": "^1.4.4",
    "vite-plugin-wasm": "^3.3"
  }
}
